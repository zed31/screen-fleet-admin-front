import { DBInterface } from './dbif';

/**
 * Class used to describe a TV
 * @class TV
 * @extends DBInterface
 */
export class TV extends DBInterface {
    Ip: string;
    Composition: string;
}
